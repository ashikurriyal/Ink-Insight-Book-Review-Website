import PropTypes from 'prop-types';

const BarChart = ({readBooks}) => {
    return (
        <div>
            <h1>Hello this is bar chart</h1>
        </div>
    );
};


BarChart.propTypes = {
    readBooks: PropTypes.object
}
export default BarChart;